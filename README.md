# SVG Icon Loader
A lightweight utility for loading inline SVG icons via:

- `<script data-icon="name">` blocks
- CDN-hosted SVG files
- Local "icons/" directory

It replaces any `.svg-icon-wrap` element with matching SVG content.

---

## 🚀 Features
- Zero dependencies
- Auto-initializes on DOM ready
- Loads icons from HTML or remote sources (CDN)
- Caching for performance
- Clean API and configurable options
- Only 2–3 KB (minified)

---

## 📦 CDN Usage (jsDelivr)

```html
<script src="https://cdn.jsdelivr.net/gh/ser-manjeet/svg-icon-loader/dist/svg-icon-loader.min.js"></script>
